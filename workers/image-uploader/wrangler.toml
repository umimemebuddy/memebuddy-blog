name = "memebuddy-image-uploader"
main = "src/index.js"
compatibility_date = "2024-01-01"

# R2 Bucket binding
[[r2_buckets]]
binding = "MEMEBUDDY_IMAGES"
bucket_name = "memebuddy-images"

# 环境变量（可选，用于删除授权）
# DELETE_TOKEN = "your-secret-token"
